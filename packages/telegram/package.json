{
  "name": "@galaxia/telegram",
  "version": "0.1.0",
  "description": "GALAXIA bidirectional Telegram bot — long polling, command router, auth, inline confirmations (Pilier 3).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@galaxia/core": "workspace:*",
    "@galaxia/agents": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
