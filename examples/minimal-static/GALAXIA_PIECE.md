# Minimal static example — GALAXIA piece

A reference piece used by `examples/` to document how an external project
plugs into a Galaxia building. This is not meant to run anything — it's
the smallest thing the onboarding pipeline should tolerate.
